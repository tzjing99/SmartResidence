export interface IntegrationFixtures {
  condoId: string;
  unitId: string;
  secondUnitId: string;
  tokens: {
    admin: string;
    owner: string;
    guard: string;
  };
  userIds: {
    admin: string;
    owner: string;
    guard: string;
  };
  emails: {
    admin: string;
    owner: string;
    guard: string;
  };
}
