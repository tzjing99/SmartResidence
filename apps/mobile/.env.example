# LAN-only dev: point at your machine's IP so a physical device on the same Wi‑Fi can reach the API.
# Find your IP: ipconfig (Windows) or ip addr / ifconfig (macOS/Linux).
# Example: EXPO_PUBLIC_API_URL=http://192.168.1.42:4000
EXPO_PUBLIC_API_URL=http://localhost:4000
