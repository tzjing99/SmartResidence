import {
  DEFAULT_LOCALE,
  type Locale,
  MESSAGES,
  normalizeLocale,
  translate,
} from '../../../web/src/i18n/messages';

export { DEFAULT_LOCALE, MESSAGES, type Locale, normalizeLocale, translate };
