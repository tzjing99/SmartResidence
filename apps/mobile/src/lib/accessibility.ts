/** iOS HIG / WCAG-aligned minimum touch target size (pt). */
export const MIN_TOUCH_TARGET = 44;

export const minTouchTargetStyle = {
  minWidth: MIN_TOUCH_TARGET,
  minHeight: MIN_TOUCH_TARGET,
} as const;
